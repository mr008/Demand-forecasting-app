# Inputs: the challenge CSVs (88 MB) are not committed; place them in data/raw/ (see README).
data/raw/
# Parquet cache regenerated by `prepare`.
data/interim/
# Outputs in data/output/ and reports/ ARE committed so reviewers can read results without running.

# environments
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# notebooks
.ipynb_checkpoints/

# tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store

# The challenge brief is not ours to redistribute.
docs/brief.pdf
